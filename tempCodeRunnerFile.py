ads = dividing(modules, NumberOfThreads)
    # threads = []
    # for i in range(NumberOfThreads):
    #     threads.append(
    #         threading.Thread(target=threadTask, args=(ModulesPerThreads[i],))
    #     )
    # for i in threads:
    #     i.start()
    # for i in threads:
    #     i.join()
    # t2 = perf_counter()
    # print(f"Time taken: {t2 - t1} seconds")
    # input("Press E